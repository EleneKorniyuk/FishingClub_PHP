<?php
/**
 * Выход из системы
 * Уничтожает сессию и перенаправляет на страницу входа
*/

session_start();

// Уничтожаем все данные сессии (забываем пользователя)
session_destroy();

// Перенаправляем на главную
header('Location: index.php');
exit;